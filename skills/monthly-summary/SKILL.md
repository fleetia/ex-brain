---
name: monthly-summary
description: 월간 작업 요약 생성. 사용자가 "/monthly-summary", "월간 요약"이라고 명시적으로 요청할 때만 실행한다. 세션 종료나 다른 작업 중에 자동으로 트리거하지 않는다.
---

# 월간 작업 요약

지식 vault(`~/KnowledgeBase`)의 주간 요약과 태스크 기록을 모아 한 달 요약을 만든다. **사용자가 직접 요청할 때만 실행한다.**

## 실행 단계

1. **대상 기간**: 기본은 직전 월(오늘이 8월이면 7월). 사용자가 `YYYY-MM`을 지정하면 그 달.

2. **소스 수집** (우선순위):
   - 1순위: `00.memory/weekly/YYYY-WNN.md` — 해당 월에 포함되는 주간 요약 (이중 압축 방지)
   - 2순위: 주간 요약이 없는 기간은 `00.memory/tasks/{done,in-progress,todo}/*.md`에서 `work-dates`(없으면 `start`/`end`)가 해당 월과 겹치는 파일로 보충
   - 하나도 없으면 "해당 월 기록이 없습니다"라고 알리고 종료한다.

3. **기존 요약 확인**: `00.memory/monthly/YYYY-MM.md`가 이미 있으면 덮어쓸지 확인한다.

4. **요약 작성**:

   ```markdown
   # {YYYY년 MM월} 월간 요약

   ## 한줄 요약
   {이번 달 전체를 한 문장으로 — 다른 직군도 이해할 수 있는 수준}

   ## 주요 작업
   - {주제별로 그룹핑, 임팩트 순 정렬}

   ## 주별 상세
   ### W{NN} ({시작일} ~ {종료일})
   - {해당 주 요약}
   ```

5. **저장**: `00.memory/monthly/YYYY-MM.md`에 저장하고 (폴더 없으면 생성) 경로와 소스 개수(주간 요약 N개 + 태스크 N개)를 보고한다.
